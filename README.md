# Jarvis

Пустой стартер Chrome-расширения. Пока только боковая панель.

```bash
npm install
npm run build
```

Chrome → `chrome://extensions` → режим разработчика → загрузить папку `dist`.
Клик по иконке открывает Jarvis справа.
